/**
 * useAuth Hook
 * 
 * Re-export dari AuthProvider untuk convenience.
 * Memudahkan import di component: import { useAuth } from '@/hooks/useAuth'
 */
export { useAuth } from '@/app/AuthProvider';
