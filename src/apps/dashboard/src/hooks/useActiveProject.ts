/**
 * useActiveProject Hook
 * 
 * Re-export dari ActiveProjectProvider untuk convenience.
 * HANYA DIGUNAKAN DI ROI SERVICE (project-level service).
 */
export { useActiveProject } from '@/app/ActiveProjectProvider';
